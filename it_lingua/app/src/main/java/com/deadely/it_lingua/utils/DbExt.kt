package com.deadely.it_lingua.utils

/*
fun UserEntity.mapToModel() = User(id, email, password, stats?.mapToModel(), active, name)
fun User.mapToDatabaseModel() =
    UserEntity(id, email, password, stats?.mapToDatabaseModel(), active, name)

fun List<StatsEntity>?.mapToModel(): List<Stat>? = this?.map { it.mapToModel() }
fun List<Stat>?.mapToDatabaseModel(): List<StatsEntity>? = this?.map { it.mapToDatabaseModel() }
fun StatsEntity.mapToModel() = Stat(id, date, countWord, countLessons)
fun Stat.mapToDatabaseModel() = StatsEntity(id, date, countWord, countLessons)
*/
